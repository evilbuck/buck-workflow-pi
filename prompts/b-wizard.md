---
description: Generate an interactive bash wizard that walks a human through steps only they can perform — credentials, dashboards, cutovers. Staged progress, confirmation gates, idempotent .env upserts, gh secret writes. Never for steps the agent can do itself.
---

# B-Wizard

$ARGUMENTS

Load and follow the `b-wizard` skill:

```
skills/b-wizard/SKILL.md
```
