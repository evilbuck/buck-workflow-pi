---
description: Create a Conventional Commits message and run git commit. Pass `force` to override protected-branch restrictions (main/master/develop). Backed by the git-commit skill.
---

# B Commit

$ARGUMENTS

Load and follow the `git-commit` skill:

```
skills/git-commit/SKILL.md
```
