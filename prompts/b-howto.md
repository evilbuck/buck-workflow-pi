---
description: Write Diátaxis how-to guides — one user action per file, numbered steps, last step Eat (the check that it worked). Writes to docs/howto/. Distinct from b-docs (why/conventions/ADRs). May load and follow b-docs when the sequence depends on an unwritten decision.
---

# B-Howto

$ARGUMENTS

Load and follow the `b-howto` skill:

```
skills/b-howto/SKILL.md
```

The how-to shape (one action per file, numbered steps, **Eat**) is in:

```
skills/b-howto/HOWTO-FORMAT.md
```
