---
description: Initialize a nested agent software factory — write factory-scoped AGENTS.md in a named harness folder, a project default, or an asked choice (never assume .claude/)
---

# B-INIT-FACTORY

$ARGUMENTS

Load and follow the `b-init-factory` skill:

```
skills/b-init-factory/SKILL.md
```
