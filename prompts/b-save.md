---
description: Deterministic OMP session checkpoint. Flags — dry-run, subject, no-retain, model, archive-inferred, run-id. Prompt fallback is /deprecated-b-save.
---

# B Save

$ARGUMENTS

The `/b-save` engine is registered by `extensions/b-save`. If this prompt runs without the extension, load `skills/b-save/SKILL.md` and follow it, or use `/deprecated-b-save` for the prompt-driven contract.

Hindsight native-memory delivery is `unsupported`. Durable `.context` apply still runs unless `--dry-run`.
