---
date: 2026-09-11
domains: [extensions, testing, security]
topics: [fix-pr, b-save, pr-20, apply-journal, index-upsert, prompt-injection]
related:
  - https://github.com/evilbuck/buck-workflow-pi/pull/20
  - https://github.com/evilbuck/buck-workflow-pi/issues/23
priority: high
status: completed
---

# fix-pr: PR #20 /b-save engine review — non-blocking fixes, blockers filed

## Validation

Review source: wooderson-ai-code-reviewer, 2026-09-11T17:36:28Z at `556f026` (re-review), carried notices from 02:10:33Z at `fb397d7`. All validated against `556f026` in worktree `../buck-workflow-pi-pr20`.

| # | source | commit | claim | verdict | disposition |
|---|---|---|---|---|---|
| 1 | review 17:36 | 556f026 | `executeRun` never invokes snapshot/evaluate/apply; `/b-save` reports completed with no durable writes | valid (design-level) | filed #23 |
| 2 | review 17:36 | 556f026 | `continueFrom` mishandles failed_apply + still-waiting resumes (wrong terminal states, duplicate effects) | valid (design-level, depends on #1) | filed #23 |
| 3 | review 17:36 | 556f026 | `abortIfBeforeChanged` skips deleted before-images (`now !== null` conjuncts) | valid | fixed in 34c6eea |
| 4 | review 17:36 | 556f026 | `composePatch` replaces memory index with one line; `upsertIndexLine` unused | valid | fixed in 34c6eea |
| 5 | review 17:36 | 556f026 | role prompts interpolate evidence without fencing | valid | fixed in 34c6eea |
| 6 | review 02:10 (carried) | fb397d7 | `prompts/b-kickoff.md` missing trailing newline | valid (nit) | fixed in 34c6eea |
| 7 | review 02:10 (carried) | fb397d7 | `current-session.json` `files_modified` omits `todo.md` | valid (nit) | fixed in 34c6eea |
| — | review 02:10 | fb397d7 | `b-loop` SKILL.md dangling cross-reference | already_done | confirmed gone at 556f026 by re-review |

## Fixes (commit 34c6eea)

- `apply.ts` `abortIfBeforeChanged`: abort on any before-image drift including deletion; regression test injects `failAfter: 0` journal then deletes the target.
- `evaluate.ts`: `upsertIndexLine` moved here (from `apply.ts`, avoiding an import cycle) and used by `composePatch` over `SaveSnapshot.memory_index_content`; `snapshot.ts` carries the raw index content.
- `roles.ts` `evidencePrompt`: explicit `<<<UNTRUSTED EVIDENCE…` / `>>>UNTRUSTED EVIDENCE;` fence; embedded marker sequences defanged to `[evidence-marker]`.
- Notices: trailing newline + session bookkeeping row.

## Sizing decision

Findings 1–2 are multi-layer orchestration (actor wiring through real role/model calls, resume semantics, manifest truth) — beyond a fix-pr single-session unit per the size gate; filed as #23 with the reviewer's two fix options and acceptance criteria. Findings 3–7 stayed in-session (guard/test/docs tweaks, one focused commit).

## Verification

- `npx vitest run extensions/b-save` → 40/40 (3 new tests: deletion abort, index upsert + dedup, fence defang).
- `npx vitest run` (full) → 446/446.
- `npx tsc --noEmit` → 102 errors identical to unmodified `556f026` (verified via stash compare); zero new.
- Pushed `556f026..34c6eea` → `origin/feat/b-kickoff-goal-prompt`.

## Notes

- Blocking-fix options (wire the machine vs soften docs) are a product call recorded in #23; not decided here.
- Worktree quirk: relative edit paths resolve to session cwd — absolute worktree paths required (recurring).
