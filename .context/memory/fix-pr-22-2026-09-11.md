---
date: 2026-09-11
domains: [extensions, workflow, testing]
topics: [fix-pr, zai-discount-banner, pr-22, pr-split, backlog-hygiene]
related:
  - https://github.com/evilbuck/buck-workflow-pi/pull/22
  - https://github.com/evilbuck/buck-workflow-pi/pull/19
priority: medium
status: completed
---

# fix-pr: PR #22 zai banner review — split + stale statuses

## Validation

Review source: wooderson-ai-code-reviewer, 2026-09-11T22:04:20Z at `edbce33` (single review).

| # | source | commit | claim | verdict | disposition |
|---|---|---|---|---|---|
| 1 | review 22:04 | edbce33 | Three subjects in one PR: banner + ZCode installer (plan forbade touching) + nvim planning artifacts | valid | fixed in fae7be0 (revert + move + delete) |
| 2 | review 22:04 | edbce33 | Memory + index still claim second review / complexity override pending | valid | fixed in fae7be0 (statuses completed) |
| 3 | review 22:04 (minor) | edbce33 | 60s interval captures session_start ctx vs fresh per-event ctx elsewhere | valid (doc nit) | fixed in fae7be0 (code comment) |

## Fixes (commit fae7be0)

- Reverted `scripts/install.mjs` + `scripts/install.test.mjs` to master — hunks byte-identical to PR #19 (verified by diff-of-diffs).
- `agent-install_instructions.md` ZCode docs existed only on this branch; moved to PR #19 as commit 020ba52 (completes the split rather than deleting reviewed content).
- Deleted `.context/2026-09-09.nvim-socket-rpc-skill/` + backlog item; stripped its todo.md row.
- Resolved stale statuses: memory file, memory index row, subject index, plan → `completed`; backlog item archived to `archive/2026-09/` with completed summary. Banner CCN 9 needs no complexity override; repo-wide baseline failure pre-dates the subject.
- Documented ctx capture asymmetry at the `setInterval` call.

## Verification

- `npx vitest run` → 530/530 (banner suite green; installer tests back at master's 7-harness state).
- Pushed `edbce33..fae7be0` → `origin/feat/zai-sale-flag`; PR #19 pushed `262206a..020ba52` with `scripts/install.test.mjs` → 71/71.

## Notes

- PR #19 itself had zero actionable findings ("change is sound") — the docs commit there is the relocation demanded by #22's split, not a #19 review item.
- `ready-for-agent`/`needs-triage` labels do not exist in the tracker yet (`gh label list` checked); #23 was filed with `bug` only.
