---
date: 2026-09-17
domains: [review, frontend, design-system]
topics: [fix-pr, pr-26, thought-dump-writer, design-language, mermaid-cdn, aria, contrast, shell-injection]
related:
  - skills/thought-dump-writer/SKILL.md
  - skills/_shared/design-brief.jsonc
  - skills/b-blueprint/references/blueprint-template.html
  - skills/b-present/references/briefing-package-patterns.md
  - presentations/2026-09-11.buck-vs-docker-orchestration-overlap/index.html
  - presentations/2026-09-11.buck-vs-docker-orchestration-overlap/adoption-plan.html
priority: high
status: completed
artifacts:
  - skills/thought-dump-writer/SKILL.md
  - skills/_shared/design-brief.jsonc
  - skills/b-blueprint/references/blueprint-template.html
  - skills/b-present/references/briefing-package-patterns.md
  - presentations/2026-09-11.buck-vs-docker-orchestration-overlap/index.html
  - presentations/2026-09-11.buck-vs-docker-orchestration-overlap/adoption-plan.html
---

# fix-pr PR #26 — design-language unification

Re-reviewed at `4bfac06` / `ca5d6dd` by Wooderson with 2 prior blockers still open:
`thought-dump-writer/SKILL.md` at repo root (unreachable) and a `TARGET=` shell-injection
pattern in its checkpoint scaffold. Several warnings (missing trailing newlines, mobile
TOC `aria-expanded` reset, `--syn-cm` contrast miss, floating Mermaid `@11` CDN range)
also persisted.

## Fixes shipped in `de3fdcf`

1. `git mv thought-dump-writer/SKILL.md skills/thought-dump-writer/SKILL.md`. Skill is now
   discoverable; full-tree grep confirms no remaining references to the old root path.
2. Rewrote the checkpoint scaffold as a fixed path-free template that takes the target
   file as `$1` argv. Added a `case` guard rejecting option-like and `.`/`..`-leading
   targets. Staging quotes the path; the commit subject uses `${TARGET##*/}` only.
   Verified with bash: rejects `-rf`, `../etc/passwd`, `foo$(...)`, no-arg, multi-arg.
3. Removed dangling `b-writer` cross-reference (skill does not exist locally).
4. `aria-expanded='false'` on TOC link close in both `index.html` and `adoption-plan.html`.
5. `--syn-cm`: `#8a8179` (3.6:1 on `--sunk`) → `#6f6862` (5.07:1 on `--bg`). Brief is the
   source of truth; both consumers regenerated via `bun skills/_shared/scripts/render-design-tokens.ts --write`.
6. Mermaid CDN pinned `mermaid@11` → `mermaid@11.12.2` in brief + both consumers.
7. Trailing newlines added to the two presentation HTMLs and `briefing-package-patterns.md`.

## Wooderson re-review (`de3fdcf`)

Itemized 6 resolved findings with file:line evidence. Verdict: no new findings, two
non-blocking carries remain (`.l1`/`.l2` brief hierarchy guidance gap, and a
Wooderson-misattributed "floating Mermaid in static pages" that I verified does not
exist — those HTMLs use static SVG, not Mermaid JS). Unit tests + CodeRabbit +
Wooderson checks all pass; PR is `MERGEABLE`.

## Verification

- `npx vitest run skills/_shared/scripts/design-language.test.ts` → 16/16 passed
- `npx vitest run` → 696/696 passed across 47 files
- All three GitHub checks green: Unit tests, CodeRabbit, Wooderson AI Code Reviewer

## Notes

- **Don't push a new SHA after Wooderson approves.** The `--reset-author` amend I tried
  produced `f8d7f75` and would have invalidated the review at `de3fdcf`. Reset back with
  `git reset --hard de3fdcf`.
- **Skill-loader parity check:** skills are discoverable by directory presence under
  `skills/`. The curated 30-skill subset at `plugins/buck-workflow/skills/` is not a
  registry — its omission of new skills (including the historic `b-capture`) is by design.
