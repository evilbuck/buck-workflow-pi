---
date: 2026-08-13
domains: [review, fixes, skill, docs]
topics: [fix-pr, pr-6, guardrails, detect-stack, functional-test-detection, b-init-guardrails, doc-regressions]
related: []
priority: medium
status: completed
---

# fix-pr: PR #6 review comments

PR: https://github.com/evilbuck/buck-workflow-pi/pull/6
Branch: `feat/deterministic-guardrails` (pushed: yes)
Commit: `0319a82` (on top of `539509c`)
Source review: `wooderson-ai-code-reviewer[bot]`, pinned to HEAD `539509c` (not stale).
No inline or conversation comments — feedback was one review body.

## Validation

| # | claim | path | verdict | disposition |
|---|-------|------|---------|-------------|
| 1 | Python/Ruby functional-test detection silently never fires | `skills/b-init-guardrails/scripts/detect-stack.ts:198-199,218-219` | valid | fixed in `0319a82` |
| 2 | `### b-research` heading replaced by guardrails note; bullets orphaned | `GLOBAL_OR_PROJECT-AGENTS.md:162-166` | valid | fixed in `0319a82` |
| 3 | Closeout list starts at `2.` (item 1 removed, no renumber) | `skills/b-build/SKILL.md:330-342` | valid | fixed in `0319a82` |
| 4a | `b-brainstorm`/`b-explore` dropped from README Skills table; separator row also dropped (table malformed) | `README.md:170-173` | valid | fixed in `0319a82` |
| 4b | `b-plan`/`b-grill-with-docs` dropped from Quick Reference Table | `docs/buck-workflow.md:377-380` | valid | fixed in `0319a82` |

All 5 valid; one localized code defect + four doc regressions introduced by this PR. Within single-session fix gate.

## Root cause (finding #1)

`RepoSignals.files` is a list of **file paths**, not directory names: `walkFiles` only
`files.push(relative(root, absolute))` on `entry.isFile() || entry.isSymbolicLink()`
(line 100); directories recurse via `visit(absolute)` and are never listed. So
`signals.files.includes("tests/e2e")` / `.includes("spec/system")` always returned
`false`, and `functional_test_cmd` silently resolved to `null` for python and ruby.
Every sibling branch in `resolveFunctionalSignals` (typescript/go/rust) uses
file-pattern/prefix matching — the python/ruby branches were the only ones using
exact directory-name `.includes()`.

Fix: `existsSync(join(signals.root, "<dir>"))` — matches the directory-existence
intent and handles empty test dirs. `existsSync`/`join` already imported.

## Verification

No unit tests for `detect-stack.ts`. Runtime fixtures via `bun detect-stack.ts`:

- Python + `tests/e2e` + `tests/functional` → `functional_test_cmd: "pytest tests/e2e"` (was `null`).
- Python + only `tests/unit` (negative) → `null` (not over-firing).
- Ruby + `spec/system` + `spec/features` → `functional_test_cmd: "rspec spec/system"` (was `null`).
- Script exits 0 (compiles, no syntax errors from edit).

Doc fixes confirmed in edit snapshots (table separator + rows restored, heading restored, list renumbered 1-4).

## Commands run

```
gh api repos/evilbuck/buck-workflow-pi/pulls/6/reviews           # fetch review body
gh api repos/evilbuck/buck-workflow-pi/pulls/6/comments          # none
git diff master...HEAD -- README.md docs/buck-workflow.md        # confirm regressions introduced this PR
bun skills/b-init-guardrails/scripts/detect-stack.ts (fixtures)  # verify code fix
git add ... && git commit -F -                                   # 0319a82
git push -u origin HEAD                                          # 539509c..0319a82
```

## Re-review round (commit 0319a82)

Bot re-reviewed 0319a82 (20:42Z): all three prior findings confirmed resolved. One NEW Low finding raised:

- **Completion Matrix table gutted** (`skills/b-review/SKILL.md:263-274`): the Step 1/2/3 example rows had been replaced by a stray `- Out-of-scope changes` bullet duplicating the one under Verification Status, leaving an empty table.

Validated against code: this was an **accidental mangling**, not an intentional drop — the bot's "intentionally dropped" inference was wrong. The completion matrix is a core concept referenced at lines 54/86/108/144/165/204 with ✅/🔄/❌ status vocabulary tied to the example rows. Fix: **restored the example rows** and removed the stray bullet (bot suggested removing the table; that would orphan the concept). Verified: `Out-of-scope changes` now 2× (prose + Verification Status); `git diff master` for the matrix region is empty.

Commit: `bb9db26` (pushed `0319a82..bb9db26`). PR comment posted.

## Final state

All review items across both review rounds resolved. PR #5 handled separately.
