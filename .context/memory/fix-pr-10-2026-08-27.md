---
date: 2026-08-27
domains: [review, security, extensions, testing]
topics: [fix-pr, pr-10, b-save-improved, path-traversal, leaf-symlink]
subject: 2026-08-26.deterministic-bsave
artifacts:
  - index.md
related:
  - fix-pr-10-2026-08-26.md
priority: high
status: completed
---

# fix-pr: PR #10 review comments (round 2)

## User Goal

Validate and act on new wooderson review comments on https://github.com/evilbuck/buck-workflow-pi/pull/10 after 8d0f1cb / 1187af6.

## What happened

Latest review on `1187af6` (2026-08-27T12:14:35Z): prior P1 slug/index fixes were incomplete because `containedContextPath` canonicalized only the parent directory and rejoined the unverified basename. A `.context/memory/*.md` leaf symlink to an external writable target passed the check; `writeFileSync` followed it.

Earlier 8d0f1cb notices still true on HEAD: applyLoose rename of a symlink source, `newBacklogItem` overwrite on slug collision, `union` dropping non-array frontmatter, and reads via `absolute()` rather than the containment helper. Nested `describe` in `wire.test.ts` is a nit.

Original P1 (slug allowlist) and P2 (index first-line idempotency) remain already_done from 8d0f1cb.

## Decision

Fix in-session (4 valid items, one file + tests). Reject leaf symlinks with `lstatSync`; canonicalize parent dirs so a directory symlink under `.context` cannot create files outside. Same helper for reads. Skip existing backlog slugs. Normalize scalars in `union`.

## What shipped

- `containedContextPath` leaf-symlink reject + parent realpath
- Reads (`updateFile`, `fixPhaseTables`, spec/iterate/todo/subject index) use that helper
- `newBacklogItem` skips when the item file exists
- `union` keeps scalar frontmatter values

## Verification

`npx vitest run skills/b-save-improved/scripts/save-apply.test.ts skills/b-save-improved/scripts/save-preflight.test.ts extensions/b-save-improved/__tests__/handler.test.ts extensions/b-save-improved/__tests__/wire.test.ts` — 64/64

Fix SHA: `9ce6da3`

## Leftover

`wire.test.ts` `lastAssistantText` describe is still nested inside `parseScribeResponse` (nit; skipped).
