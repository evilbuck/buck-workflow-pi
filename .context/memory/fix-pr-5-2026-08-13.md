---
date: 2026-08-13
domains: [review, fixes, docs]
topics: [fix-pr, pr-5, b-plan, standalone-bootstrap, agent-install, plugin-marketplace, context-validate, draft-commit-gitignore]
related: []
priority: low
status: completed
---

# fix-pr: PR #5 review comments

PR: https://github.com/evilbuck/buck-workflow-pi/pull/5
Branch: `feat/standalone-b-plan-bootstrap` (pushed: yes)
Commit: `f49bd89` (on top of `e7024e0`)
Source review: `wooderson-ai-code-reviewer[bot]`, pinned to HEAD `e7024e0` (not stale). Verdict: Approve with minor nits.
No inline or conversation comments — feedback was one review body.

## Validation

| # | claim | path | verdict | disposition |
|---|-------|------|---------|-------------|
| 1 | Stale `@buckleyrobinson` publisher qualifier after evilbuck migration | `agent-install_instructions.md:389` | valid | fixed in `f49bd89` |
| 2a | Links to files not in this PR: `todo.md`→`items/first-npm-publish.md`, `index.md`→`npm-publish-readiness-2026-08-12.md` | `.context/backlog/todo.md:5`, `.context/memory/index.md:1` | skipped | `.context/**` session artifacts (skill rule); targets belong to separate, deliberately-excluded npm-publish workstream; do not fail any check |
| 2b | `draft-commit.md` referenced by `current-session.json` but absent from PR | `.context/workflow/current-session.json` | invalid | gitignored by design (`.gitignore:9-10`) — transient commit-message draft, never committed |

Only finding 1 actionable; trivial 1-line doc change. Within fix gate.

## Evidence on finding 2 (reviewer's mechanism was wrong)

- `draft-commit.md` is gitignored: `.gitignore:9-10` = `.context/draft-commit.md`, `.context/*/draft-commit.md`. It is a transient commit-message draft never committed; referencing it from workflow state is normal, not a broken link.
- The reviewer claimed the npm-publish links "will likely fail `npm run context:validate`." Verified: `npm run context:validate` does **frontmatter validation only**, returns `0 errors, 71 warnings, exit 0`. It does NOT check markdown link integrity. The dangling links fail no check.
- The npm-publish targets are forward-references to a parallel workstream the build memory records as "unrelated user changes, deliberately not modified." The todo item is literally marked "blocked on test gate" — pending work whose files get created when that workstream lands. Removing the links would lose backlog tracking.

## Verification

- Docs-only change (`.md`); deterministic-check contract is docs-only → skipped (one-line note).
- Diff confirmed: only the publisher qualifier changed.

## Commands run

```
gh pr view 5 ... --json ...                      # metadata, head_sha = e7024e0
gh api .../pulls/5/reviews                       # review body, pinned to e7024e0
grep first-npm-publish/npm-publish-readiness     # locate actual link lines
grep draft-commit .gitignore                     # confirm gitignored by design
npm run context:validate                         # 0 errors, exit 0 (frontmatter-only)
git commit -F - ; git push                       # e7024e0..f49bd89
```

## Follow-ups

None. All actionable items resolved. Findings 2a/2b are documented as skipped/invalid with evidence (skill rule + gitignore + context:validate behavior).
