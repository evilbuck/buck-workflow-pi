---
date: 2026-07-25
domains: [review, fixes]
topics: [fix-pr, pr-3, kamal, b-commit-improved, commit-preflight]
related: [".context/memory/b-commit-improved-2026-07-25.md", ".context/memory/b-kamal-release-extension-2026-07-24.md"]
priority: medium
status: completed
subject: fix-pr-3
artifacts: ["extensions/b-commit-improved/index.ts", "extensions/b-commit-improved/__tests__/wire.test.ts", "extensions/b-kamal-release/index.ts", "extensions/b-kamal-release/__tests__/wire.test.ts", "skills/git-commit-improved/scripts/commit-preflight.ts"]
---

# fix-pr: PR #3 review comments

## PR
- URL: https://github.com/evilbuck/buck-workflow-pi/pull/3
- Branch: feat/deterministic-git-commit (pushed: yes)
- Reviewer: @wooderson-ai-code-reviewer @ 6758f5e
- 3 findings, all valid, fixed in 82346ae

## Validation table

| # | Claim | Verdict | Disposition |
|---|---|---|---|
| 1 | `commit-preflight.ts:3873` uses `bash -c` with `.context/` glob; replace with `readdirSync` | valid | fixed in 82346ae (replaced with `readdirSync(".context/")` + regex filter, keep `.context/` prefix) |
| 2 | `b-kamal-release` strips `v` after `resolveVersion` already added it, so `--version=` mismatches the tag | valid | fixed in 82346ae (`const version = tag;` — no second strip) |
| 3 | `b-commit-improved --dry-run` + no model prints "Drafted to ${path}" though no draft was written | valid | fixed in 82346ae (branch on `opts.dryRun`; surface real reason) |

## Commands run

- `pr://3` + `pr://3?comments=1` — read PR metadata + review
- `npx vitest run extensions/b-kamal-release extensions/b-commit-improved` — 40/40 pass (38 baseline + 2 new regression tests)
- `bun skills/git-commit-improved/scripts/commit-preflight.ts --force` — smoke tested on real `.context/2026-07-25.git-commit-improved`, resolved correctly
- `git push -u origin HEAD` — pushed 82346ae to feat/deterministic-git-commit

## Notes

- Reviewer-cited line numbers were inflated (e.g. `commit-preflight.ts:3873` when file is 201 lines). Real locations verified via `grep` before edits.
- The `ls -dt` mtime-desc semantics became lexicographic-desc on the date prefix. For `YYYY-MM-DD.*` folders, ordering matches in practice (date prefix IS the creation order). Documented in the comment.
- `b-auto-fix` test suite has 23 pre-existing failures (`Bun is not defined` — vitest runner, not node). Out of scope; not addressed.
- Ponytail note: the new helper `makeKamalRepoWithTag` was rejected as a one-line delegate per project rule. Reused `makeKamalRepo` + inlined the `git tag` call in the new test.
