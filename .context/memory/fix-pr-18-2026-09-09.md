---
date: 2026-09-09
domains: [review, fixes, installer, testing, skill]
topics: [fix-pr, pr-18, dangling-symlink, verify, installer, b-capture, concurrent-writers]
related: ["scripts/install.mjs", "scripts/install.test.mjs", "skills/b-capture/SKILL.md", "docs/buck-workflow.md", "https://github.com/evilbuck/buck-workflow-pi/pull/18"]
priority: high
status: completed
artifacts:
  - scripts/install.mjs
  - scripts/install.test.mjs
  - skills/b-capture/SKILL.md
  - docs/buck-workflow.md
  - .context/memory/fix-pr-18-2026-09-09.md
---

# PR #18 review fixes

PR: https://github.com/evilbuck/buck-workflow-pi/pull/18
Branch: `feat/iterative-note-taking`
Fix commits: `f08ec3bea0bdffd2f508f72d7166bc00f32d060b` (dangling symlink), `63cb8a4` (capture entry files)
Pushed: yes

## Validation

| # | Review source | Claim | Verdict | Disposition |
|---|---|---|---|---|
| 1 | Review `5145776483` at `9d944ed6` | PR surfaces and documentation are internally consistent; three notices were described but not concrete findings. | nit/informational | No issue filed; no code change. |
| 2 | Review `5145785163` at `9d944ed6`; reiterated by `5156811085`, `5156975606`, `5156990178` | Concurrent `b-capture` writers could overwrite shared capture files. Cited line 557 on a 92-line file. | valid (re-opened on second pass) | First pass marked `already_done` from review `5156847721`. Later reviews at `f08ec3b` / `10db37f` still listed it. HEAD still had step 2 write the shared log while step 4 forbade it. Fixed in `63cb8a4`. |
| 3 | Review `5156811085` at `79dee5a` | Installer and response-style changes are sound; PR title/body omit unrelated landed work. | nit/informational | No code change; description drift is not a defect requiring an issue. |
| 4 | Review `5156847721` at `79dee5a` | `--verify` reports dangling links, but a normal install treats them as missing and `symlinkSync` raises `EEXIST`. | valid | Fixed in `f08ec3b`: `ensureSymlink` uses `lstatSync` so dangling directory entries are classified and relinked. |

No inline review threads or conversation comments were returned by the GitHub APIs.

## Verification

- Reproduction before fix: `existsSync` returned `false` for a dangling symlink, direct `symlinkSync` returned `EEXIST`.
- `npx vitest run scripts/install.test.mjs` → 69 passed.
- `npx vitest run` → 27 files, 474 tests passed.
- `npx vitest run --coverage --coverage.reporter=lcov` → 27 files, 474 tests passed.
- `git diff --check` → clean.
- Complexity: `lizard` is not installed on PATH; equivalent `uvx lizard` completed. Changed `scripts/install.mjs` reports `ensureSymlink` CCN 9, within the configured maximum of 10.

## Closeout

One focused fix commit was pushed to the PR branch. No follow-up issues were filed.

## Second pass (2026-09-09, post-`10db37f`)

Re-ran `/skill:fix-pr` after reviews `5156975606` (`f08ec3b`) and `5156990178` (`10db37f`). No inline or conversation comments. Latest extract review: findings `[]`, two still-standing informational items.

| # | Claim | Verdict | Disposition |
|---|---|---|---|
| 2 | Concurrent writers can lose a dump | valid | `63cb8a4`: each dump is `notes/entries/<NNN>.md`; mainline appends a log pointer; subagents never touch shared files |
| 3/5b | PR title/body omit installer and response-style work | nit | No issue; not a code defect |
| 4 | Dangling symlink `EEXIST` | already_done | `f08ec3b` + tests at `scripts/install.test.mjs:186` and `:715` |
| 6b | `.context` status still `active` on landed work | skipped | `.context/**` only |

Verification: skill re-read for internal consistency (step 2 exclusive entry file, step 4 mainline log). Docs-only `.md` change — guardrails skipped.
