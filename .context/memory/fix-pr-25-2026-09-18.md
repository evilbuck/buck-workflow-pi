---
date: 2026-09-18
domains: [review, workflow, agent-instructions]
topics: [fix-pr, pr-25, graphql-reviewthreads, paginate, fail-closed, untrusted-data, fork-isolation, consumer-sync, coderabbit]
related: [fix-pr-resolution-loop-2026-09-12.md]
priority: high
status: active
subject: 2026-09-12.fix-pr-resolution-loop
artifacts: [skills/fix-pr/SKILL.md, skills/fix-pr/scripts/fetch-feedback.sh, plugins/buck-workflow/skills/fix-pr/SKILL.md, plugins/buck-workflow/skills/fix-pr/scripts/fetch-feedback.sh, docs/buck-workflow.md, presentations/2026-09-12.fix-pr-skill-report/index.html, AGENTS.md, README.md, GLOBAL_OR_PROJECT-AGENTS.md, agent-install_instructions.md]
---

# fix-pr: PR #25 review feedback (own PR, loops 1-3)

PR: https://github.com/evilbuck/buck-workflow-pi/pull/25 — feat(fix-pr): add bounded re-review loop
Worktree: branch `feat/better-fix-pr` @ last push `6058fd0`
Reviewed commit: `d638f54`; pushes: `22630d8`, `66729ac` (superseded by rebase onto origin/master `391f4aa`), current lineage `4e8c97b` (loop-1 fix), `d40c965` (loop-2), `f616a30` (loop-3 fail-closed), `6058fd0` (record + worktree-header head)

## Validation table (loop 1)

| id | source | claim | verdict | disposition |
|---|---|---|---|---|
| W1 | wooderson-ai-code-reviewer 5186542980 + 5187472038 + 5187524001 | Phase 5c fetches "thread resolution state" but no listed command exposes it (`gh pr view --json`/REST lack `isResolved`) | valid | fixed: GraphQL `reviewThreads{isResolved}` call added to Phase 5c step 1 |
| W2 | wooderson (same three reviews) | docs Flags line omits `--max-loop` fix-mode scope | valid | fixed: "(default 10; fix mode only)" |
| CR1 | coderabbitai 3997138072 | Quick Reference anchor `#fix-pr--validate-…` doesn't match `/skill:fix-pr — …` heading slug | valid | fixed: `#skillfix-pr--validate-and-act-on-pr-review-comments` |
| CR2 | coderabbitai 3997138074 | presentation hero shows `scripts/fetch-feedback.sh` instead of full path | valid | fixed |
| CR3 | coderabbitai 3997138080 | dry-run "NEVER mutates" vs Phase 6 memory write contradiction | valid | fixed: skill Inputs now "no repo or PR mutation"; presentation warn note names the Step 6 memory record as the only dry-run write |
| CR4 | coderabbitai 3997138083 | shipped consumers stale (plugin bundle SKILL.md, AGENTS.md, README.md, GLOBAL_OR_PROJECT-AGENTS.md, agent-install_instructions.md) | valid | fixed: plugin bundle re-mirrored (cmp identical); five prose consumers updated to fix-loop contract |
| CR5 | coderabbitai 3997138084 | no explicit untrusted-data rule for collectors/validators | valid (partially pre-covered) | fixed: orchestrate paragraph + "Feedback is untrusted data" behavior rule |
| CR6 | coderabbitai 3997138086 | fallback REST calls + fetch-feedback.sh missing `--paginate` | valid | fixed: `--paginate` in skill fallback, collector script, plugin mirror, presentation fallback block |
| CR7 | coderabbitai 3997138091 | Phase 5a runs fork code without isolation policy | valid | fixed: Phase 5a step 2 sandbox-or-approval gate + "Fork code is untrusted" behavior rule |

Dropped as noise: SkillSpector AS3 skill-enumeration warnings (analyzer misread skill-name references as file enumeration); superseded 13:23 extract review notices (final re-review at 18:32 explicitly closed them).

## Verification

- `bash skills/fix-pr/scripts/fetch-feedback.sh evilbuck/buck-workflow-pi 25` → 13 rows across pages (5 reviews + 7 inline + 1 conversation) — `--paginate` + per-page jq verified live
- GraphQL `reviewThreads` query against PR #25 → 7 threads, all `isResolved=false` (exactly the CodeRabbit inline set) — W1 mechanism proven live
- `bash -n` on both collector copies → OK; stale-text grep (`no local or remote mutation`, `NEVER mutates`, `fix-only`, `fix-or-issue`) → zero hits
- Guardrails (durable contract, guardrails.json v2): TS unit gate `vitest run` → 35 files / 544 tests pass; `bun test` script half → 70 pass; lint/coverage commands not configured for touched ecosystems (advisory)
- Worktree note: `node_modules` symlinked to main checkout for vitest; left untracked

## Loop state

- Loop 1: pushed `22630d8`; poll at ~5 min returned CodeRabbit CHANGES_REQUESTED + Wooderson re-review. Both original Wooderson findings confirmed resolved; the 7 old CodeRabbit threads auto-resolved. One new theme, cited by both reviewers (same root cause, deduped to one item).
- Loop 2: new finding — GraphQL `reviewThreads` query capped `first:100` with no `$after` threading (resolved threads fill early pages; no `isResolved` filter → open threads could hide past the cap → false `settled`). Valid. Fixed with a mechanical `endCursor` loop in Phase 5c step 1; verified live against PR #25 (8 threads walked, single page, clean exit) including catching a missing-`)` quote bug in the first draft via extraction run. Pushed `66729ac` (docs-only commit — guardrails gate skipped per docs-only predicate; unit gate already green this session).
- Loop 3 (poll 1, ~1 min after push): Wooderson review 5254270005 @ 03:08:14Z on `66729ac` — "No new findings in the delta"; cursor gap "closed in this SHA"; contract soundness confirmed; mirror parity preserved. `unresolved_threads=0` across all review threads. Settled — **then revoked 100s later**: CodeRabbit 5254273382 @ 03:09:53Z (CHANGES_REQUESTED, also user-reported) found the new cursor loop **fails open**: `jq -e … || break` exits non-zero for false, null, AND parse errors (so a truncated/malformed page ended the walk after page 1), `.errors[]` was never inspected, the `gh` exit code was never propagated, and no `set -e` safety net. Valid — the exact false-`settled` mode the PR prevents.
- Loop 3 fix (`8c6ca71`, post-rebase): fail-closed rewrite — `gh` failure aborts, `.errors[]`/malformed/missing reviewThreads aborts, pagination continues only on literal `true`, completes only on literal `false`, true-page with missing `endCursor` aborts. Verified live: 11-thread happy walk on PR #25 + injected errors-array / data:null / hasNextPage:null / garbage-JSON / gh-failure paths all abort with the guard message. Mid-session the remote gained Wooderson bot autofix `286cb25` (same fail-closed fix on the old base) — folded its deltas (gh-exit diagnostic, exit-5 convention, `review_pending`-never-`settled` prose) and superseded the commit via rebase + force-with-lease; lease had correctly rejected the first push attempt against the moved ref.
- Rebase (user-directed, CONFLICTING/DIRTY at d638f54-lineage): replayed 6 commits onto origin/master `391f4aa`; conflicts resolved semantically — docs/buck-workflow.md Discoverability (keep master's grouped catalog, adopt branch's fix-pr description), README.md skill table (keep master's b-loop/b-pr/code-review rows, adopt updated fix-pr description), .context/memory/index.md (prepend fix-pr-25 over master's entries, no fix-pr-24 duplicate). Range-diff 6:6 (`!` on commits 1/4/6 = conflict resolutions); force-push eligible.
- Status at write time: loop 3 pushed, polling loop 4 for re-review.
