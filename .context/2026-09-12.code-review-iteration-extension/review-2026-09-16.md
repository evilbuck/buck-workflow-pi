## Plan Path Review: code-review-iteration-extension (post-iterate)

### Plan Source
- File: `.context/2026-09-12.code-review-iteration-extension/iterate-code-review-iteration-extension.md` (acceptance contract for this pass); brainstorm `brainstorm-code-review-iteration-extension.md` still in force for loop ownership.
- Goal: close the 2026-09-16 iterate (2 Critical + 8 Warnings + Found A/B) without false-green gates.
- Baseline: branch `feat/code-reviewer-extension` at `b63e2d4`; uncommitted iterate delta vs HEAD (`+484/-105` in extension + tests).

### Evidence Sources
- Git status: unstaged extension + test edits; untracked iterate artifact + session memory.
- Recent commits: `b63e2d4 feat(review): add isolated code-review iteration command`, `c70e853 docs(review): draft isolated review iteration extension`.
- Modified: `extensions/code-review-iteration/{loop,catalog,findings,policy,git-ops,index,report,run-state}.ts` + matching `__tests__`.
- Guardrails: durable v2; `vitest run` pass; `diff-cover` 89% vs `origin/master`; lizard on extension source (genuine CCN ≤ 10; `parseReviewerOutput@282-683` is a lizard misparse).
- Fingerprint: `worktreeFingerprint` identical across 1.1s with tracked dirty + untracked (stash sha 40 chars, dates pinned).

### Completion Matrix

| Step | Status | Evidence |
|------|--------|----------|
| C1 invalid reviewer payload must not terminalize `clean` | ✅ complete | `loop.ts:289-293` returns `{ error }` on any `validation.errors`; tests `loop.test.ts:440-461` (`findings: "none"` and unknown `c9` → `failed`) |
| C2 catalog-preflight after resume must terminalize on disk | ✅ complete | `loop.ts:607-611` → `terminalResult`; test `loop.test.ts:463-481` (`state.json` status `failed`) |
| W1 fixer-created files enter the checkpoint | ✅ complete | `loop.ts:445,461-468` pre/post untracked diff; test `loop.test.ts:484-503` |
| W2 persist post-rebase `last_head` / fingerprint | ✅ complete | `loop.ts:623-627` + non-clean `terminalResult` 582-588; test `loop.test.ts:506-529` |
| W3 `:thinking` suffix on reviewer selector | ✅ complete | `catalog.ts:203-204,257-268` `baseSelector`; test `catalog.test.ts:186-202` |
| W4 `changed_paths` are filesystem paths | ✅ complete | `changedPathsSince` + `fixerRecord`; same test as W1 |
| W5 uncatalogued explicit fixer: no inherited thinking table | ✅ complete | `loop.ts:392-393`; test `loop.test.ts:532-542` (`thinkingLevel` null) |
| W6 patch ≥ 90% and iterate CCN ≤ 10 | 🔄 partial | Extension CCN split landed (`validateReproduction` CCN 8, `runReviewerPass` CCN 4). Patch **89%** (124/1234 missing). Dominant hole `index.ts` 61.6%. Iterate claimed 91.97% — not reproduced. |
| W7 loader/injection env denylist | ✅ complete | `policy.ts:33-34,239-240`; test `policy.test.ts:113-133` |
| W8 check runner / byte cap / drop duplicate effective_* | 🔄 partial | Shell-syntax skip via `checkContractCommands` (`index.ts:304`). Byte cap by `buf.length` (`policy.ts:247-265`). `effective_model` gone. **But** `readCheckContractCommands` reads `test_cmd` not `test_runner`, so this repo's contract never runs (see Critical 1). |
| Found A fingerprint time-stability | ✅ complete | `git-ops.ts:25-28` pinned dates; eval: identical fingerprints 1.1s apart with tracked dirty |
| Found B checkpoint before fetch/rebase | ✅ complete | `loop.ts:176-193` before `fetchBase`. Residual: `rebaseOntoFetched` still `--autostash` (`git-ops.ts:114`) |

### Review Axes
- Spec axis worst finding: live `/code-review` never executes this repo's `test_runner` — `readCheckContractCommands` looks up `test_cmd`, gets `[]`, `runChecks` fail-opens `passed: true` (`index.ts:149-157,307`). False-green fixer checkpoints.
- Standards axis worst finding (parallel sub-agent `StandardsReview-2`, 0.84): same fail-open at `index.ts:307`. Notes: mid-codepoint byte-cap (`policy.ts:265`), `baseSelector` not reused (`catalog.ts:291`), leftover `--autostash` (`git-ops.ts:114`).
- Cross-axis ranking: none (per-axis reporting only). Both axes independently named the check-contract fail-open.

### Verification Status
- Goal achieved: partial — iterate C1/C2 and most warnings are in source and tested; the deterministic check contract still does not run in production.
- User goal: partially met — bounded loop exists, but Fixer "verified" commits can land without `vitest run`.
- Scope adhered: yes (extension-only + tests + `.context`).
- Out-of-scope changes: none.

### Guardrails Verdict
- Contract: durable
- Contract version: 2
- Status: fail
- Gates: `unit_test_gate=pass`; `functional_test_gate=skipped`; `lint_gate=skipped` (`lint_cmd: null`); `patch_gate=fail` (89% < 90); `global_ratchet=pass` (80.8% > 54.9 baseline); `complexity_gate=fail` on **pre-existing** master-side hotspots (`render.py:validate` 28, `runBSaveImproved` 27, `parseModelResponse` 16, `import-projects.ts:parseArgs` 68) — **not** this iterate's functions. Lizard also misparses `parseReviewerOutput` as spanning 282-683.
- Patch detail: `index.ts` 61.6%, `loop.ts` 92.0%, `policy.ts` 91.8%, `git-ops.ts` 94.8%, `catalog.ts` 98.3%, `run-state.ts` 100%.

### User Goal Analysis
- Goal: bounded Reviewer→Fixer→fresh-Reviewer loop that only checkpoints verified fixes.
- Met: C1/C2 false-green holes in payload/resume; fixer new-file checkpoint; resume fingerprint; thinking-suffix routing; env denylist; fingerprint clock pin; checkpoint-before-rebase.
- Partial: check contract in production; patch gate; leftover autostash.
- Missing: `test_runner` mapping.
- Verdict: partially met.

### Documentation Impact
- How-to `docs/howto/run-code-review.md:10` still says fetch/rebase then untracked prompt; implementation checkpoints dirty start *before* fetch/rebase.
- Eat step still says "effective model"; `PassReviewRecord` dropped `effective_model`.
- Recommended: `/b-docs` first if both flagged — but correctness iterate comes first. Non-blocking.

### How-to Impact
- Existing `/code-review` how-to is stale on checkpoint order (above).
- Recommended: `/b-howto` after the iterate, or rolled into `/b-docs`.

### Issue Classification
- In-plan issues (implementation defects → `/b-iterate`):
  1. Critical: `test_cmd` vs `test_runner` + fail-open (`index.ts:149-157,307`)
  2. Warning: patch 89% < 90%
  3. Warning: leftover `--autostash`
  4. Warning: mid-codepoint excerpt cap
- Out-of-plan issues (scope discoveries → fresh `/b-plan`):
  - `LoopContext` still 19 `!` assertions (recorded 2026-09-12; not re-litigated)
  - `catalogAllows` vs `tierAtLeast`; inlined `blocking` predicate; `state*` wrappers
  - `reviewerThinking` / `catalogEntryFor` duplicate `split(":")` instead of exporting `baseSelector`
  - Repo-wide complexity baseline drift (master-side functions) — re-baseline is a separate `/b-init-guardrails` refresh, not this subject

### Verdict
**Needs work** — driven by in-plan Critical 1 (false-green check contract) and W6 patch gate still failing.

### Recommended Next Step
`/b-iterate` on `.context/2026-09-12.code-review-iteration-extension/iterate-code-review-iteration-extension-2.md`. Then re-run `/b-review`. Do not `/b-save` / `/b-commit` until the check-contract reader is fixed and patch ≥ 90%.
