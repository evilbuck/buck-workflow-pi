# Catalog Drift Audit — Skills / Commands / Extensions

**Repo**: /home/buckleyrobinson/projects/development_tools/buck-workflow-pi
**Branch**: master @ 7dc2aaf
**Source of truth**: `local://inventory.md`
**Scope**: README.md, docs/buck-workflow.md, docs/extension-loading.md, AGENTS.md, docs/b-flow.md, GLOBAL_OR_PROJECT-AGENTS.md, scripts/install.mjs, plugins/buck-workflow/.codex-plugin/plugin.json, plugins/buck-workflow/skills/
**Note**: This catalog layer covers **names + description rows**. Per-skill per-task usage evidence is the sibling `notes-content.md`.

## Findings (priority ordered)

| id | classification | evidence | recommended action |
|---|---|---|---|
| C1 | KEEP | `README.md:75-79` Project Structure names the layers; `README.md:220-250` Prompt Templates table (31 rows), `README.md:262-267` Extension-Backed Commands (6 rows), `README.md:273` Pure Prompt Commands (`/b-save`), `README.md:279-340` Skills table (~53 rows). Counts match `local://inventory.md`: 43 prompts (all symlinked to `prompts/`) and 64 skills. | KEEP — README catalog layer is the most complete machine-readable inventory; the prompt-template table + skill table together cover all surfaces. |
| C2 | CATALOG-GAP | README "Prompt Templates" table at `README.md:220-250` (31 rows) is missing 12 prompt-template rows that exist in `prompts/` and `commands/`: `b-save`, `b-build-hard`, `b-docs`, `b-pr`, `git-clean-orphans`, `product-tour`, `omp-goal`, `omp-workflow`, `omp-orchestrate`, plus the 3 covered by Extension-Backed + Pure Prompt sub-tables (`b-save-improved`, `b-commit-improved`, `b-pr-improved`, `b-kamal-release`). After sub-tables, 9 prompt entries remain uncatalogued in README: `b-save` (Pure Prompt covers it), `b-build-hard`, `b-docs`, `b-pr`, `git-clean-orphans`, `product-tour`, `omp-goal`, `omp-workflow`, `omp-orchestrate`. The Skills table covers 4 of those (`b-save` line 308, `b-pr` line 315, `product-tour` line 338, `git-clean-orphans` line 333). Net gap: 5 prompt entries (`b-build-hard`, `b-docs`, `omp-goal`, `omp-workflow`, `omp-orchestrate`) are in neither Prompt Templates nor any sub-table. | Either (a) merge the three sub-tables into one Prompt Templates table — same shape as `docs/buck-workflow.md:38-75` "Runtime package mapping" — or (b) document that the sub-tables ARE the prompt-template catalog. Current state hides 5 prompt entries from a high-traffic catalog. |
| C3 | CATALOG-GAP | `docs/buck-workflow.md:38-75` "Runtime package mapping" table lists 36 rows. It does not list `b-build`, `b-explore`, `b-research`, `b-brainstorm`, `b-iterate`, `b-commit` as separate rows. Those six are catalogued in the Quick Reference Table at `docs/buck-workflow.md:301-460` instead. | KEEP — the Runtime package mapping is the per-prompt surface contract (file paths, OMP↔Pi translation), not the entrypoint list. The Quick Reference Table at `docs/buck-workflow.md:301-460` covers the missing six. |
| C4 | CATALOG-GAP | `docs/buck-workflow.md` Quick Reference Table (`docs/buck-workflow.md:301-460`) lists ~38 components. Compared to `local://inventory.md` (64 skills), it is missing: `b-grill`, `b-grill-auto`, `b-grill-me`, `b-grill-with-docs`, `b-create-styleguide`, `b-create-ux-guide`, `b-backlog`, `b-arch-qa`, `b-blueprint`, `b-issue-create`, `b-auto-fix`, `b-memory-import`, `b-hindsight-import-projects`, `b-nasa-prd`, `b-pr`, `b-pr-review-2-issues`, `b-pr-improved`, `b-kamal-release`, `b-commit-improved`, `b-save-improved`, `codebase-design`, `crawl4ai`, `cross-platform-pi-omp-loading`, `design-brief`, `git-clean-orphans`, `llm-wiki-vault`, `manage-herdr-panes`, `node5-code-review`, `pi-rpc`, `product-tour`, `rails-app`, `run-in-idle-pane`, `skill-explainer`, `thought-dump-writer`, `writing-for-agents`, plus the three `omp-*` stubs and `b-fix-rebase-conflict` / `b-howto` / `b-init-factory` / `b-init-tracker` / `b-init-guardrails` / `b-iterate` / `b-grill-me` / `b-grill-with-docs`. **Most of these are covered by the Runtime package mapping table at lines 38-75.** | KEEP — the Quick Reference Table is a curated subset; full skill list lives in `README.md:279-340`. Both catalogs are correct in scope. |
| C5 | CATALOG-GAP | `AGENTS.md:94-100` "Available Skills" is **8 lines and only names 3 surfaces**: `llm-wiki-vault` (line 96), `b-save` (line 98), `b-memory-import` (line 100). The section header is misleading — AGENTS.md does not actually catalog the 64 skills shipped under `skills/`. By contrast, `GLOBAL_OR_PROJECT-AGENTS.md` has no "Available Skills" section at all (covers workflow conventions, not surface catalogs — verified by full read). | Either (a) rename `AGENTS.md:94` "Available Skills" → "Notable Skills" and add a one-line pointer to `README.md` and `docs/buck-workflow.md` for the full list, or (b) leave as-is and add a single sentence "Full skill catalog: README.md § Skills and docs/buck-workflow.md § Quick Reference Table." Option (b) preserves the curated callout. |
| C6 | CATALOG-GAP | `GLOBAL_OR_PROJECT-AGENTS.md` does not list any surface names directly. The workflow-steps line names the b-* stages in a single line: `b-brainstorm → b-research → b-plan → b-present → b-build → b-build-hard → b-review → b-iterate → b-docs → b-save → b-commit`. No other surface list appears. | KEEP — by design (per `AGENTS.md:20-25` "Bootstrap vs Project AGENTS.md" section). Bootstrap file is intentionally generic and avoids surface catalogs. Confirmed against `AGENTS.md` and `docs/buck-workflow.md`. |
| C7 | KEEP | `scripts/install.mjs:42-93` HARNESSES array declares 8 harnesses (`pi`, `omp`, `claude`, `codex`, `opencode`, `cursor`, `grok`, `zcode`). Per-harness surfaces: `bootstrap` universal except Cursor; `commands` + `skills` for `claude`, `opencode`, `grok`; `skills` only for `zcode`; nothing for `pi` / `omp` (loaded via package manager). Compared to `docs/buck-workflow.md:163-176` (8 harnesses), `docs/extension-loading.md:228-254` (8 harnesses), and `README.md:155-177` (8 harnesses): all four are consistent. | KEEP — all four catalogs agree; installer is source of truth, docs are mirrors. |
| C8 | KEEP | `plugins/buck-workflow/.codex-plugin/plugin.json:1-37` declares `"skills": "./skills/"` (no commands listed; Codex invokes skills via `$name`). Bundle at `plugins/buck-workflow/skills/`: 31 entries (verified by `ls` glob) vs 64 canonical skills. `plugins/buck-workflow/README.md:14-18` says "every entry is either a byte-for-byte recursive copy of the same-named canonical skill, or one of the two Codex-only skills (`b-build-hard`, `b-commit`) with no canonical twin." 2 Codex-only + 29 canonical-copies = 31 (matches). `scripts/codex-plugin.test.ts` enforces this parity per `plugins/buck-workflow/README.md:32-34`. | KEEP — explicit, test-enforced, curated bundle; intentional subset. README documents the rule. |
| C9 | STALE-REF | `README.md:294` "b-diagnose" entry in Skills table describes it as "(skill-only, no slash wrapper)" but `prompts/b-diagnose.md` exists and `/b-diagnose` is correctly listed in the Prompt Templates table at `README.md:237`. The Skills-table annotation is incorrect. | STALE-REF — fix the parenthetical or remove the duplicate Skills-table row (line 294 description duplicates line 237). |
| C10 | TOMBSTONE-OK | `docs/b-flow.md:1-9` says "XState `/b-flow` was unwired 2026-06-01 and deleted 2026-09-20." All `.context/2026-05-08.b-orchestration-extension/` and `.context/2026-06-01.deprecate-b-flow/` artifacts retain b-flow references intentionally (per deprecation plan `Out of Scope: Historical .context/** artifacts (preserved as record)`). Live references: `README.md:350` ("Removed: ... `b-flow`") and `docs/buck-workflow.md:87-89, 149, 513, 1947` correctly describe the deletion. No live `/b-flow` command exists (verified by `ls extensions/` — no `b-flow/` dir; `ls prompts/` — no `b-flow.md`; `ls commands/` — no `b-flow.md`). | KEEP — tombstones documented in `docs/b-flow.md`, `docs/buck-workflow.md`, `README.md`, `docs/extension-loading.md`. Current state is correct. |
| C11 | TOMBSTONE-OK | `README.md:350` "Removed: `/b-mode`, plan-mode write guards, `/b-save` as an extension command, `b-flow`. Unwired: `b-grill-auto` extension command, tmux status, session-state injection." `docs/buck-workflow.md:510-515` repeats the same list. Both match `extensions/index.ts:6-13` wire imports (no b-flow, no b-mode, no tmux, no session-state, no grill-me-dialog, no grill-auto). All four catalogs agree. | KEEP — tombstone list is consistent across README, docs/buck-workflow.md, and the live wire surface. |
| C12 | TOMBSTONE-OK | `/skill:b-loop` references in live docs: `AGENTS.md:70` "Skills may reference Pi-specific syntax (e.g. `/skill:b-phase`, `/b-save`)" — uses `b-phase` as example, not `b-loop`. `docs/buck-workflow.md:65` lists `b-grill` (different skill). No live `/skill:b-loop` reference exists in `README.md`, `AGENTS.md`, `docs/buck-workflow.md`, `docs/extension-loading.md`, `GLOBAL_OR_PROJECT-AGENTS.md`, or `scripts/install.mjs`. `skills/b-loop/SKILL.md` was deleted 2026-09-20 per `docs/adr/0002-observably-invoked-happy-path-loop.md:3` and `.context/memory/cleanup-dead-b-loop-2026-09-20.md:15-17`. | KEEP — `/skill:b-loop` was removed 2026-09-20. `.context/` mentions are historical record, not live. |
| C13 | TOMBSTONE-OK | `memory-manager` skill deletion: `.context/memory/pi-opencode-mapping-2026-04-16.md:25-26` and `.context/backlog.md:101` both record deletion on 2026-04-16. No live reference to `memory-manager` in `README.md`, `AGENTS.md`, `GLOBAL_OR_PROJECT-AGENTS.md`, `docs/buck-workflow.md`, `docs/extension-loading.md`, or `scripts/install.mjs`. The `skills/memory-manager/` directory does not exist (verified by `ls skills/`). | KEEP — historical-only references in `.context/` are correct record; no live stale refs. |
| C14 | TOMBSTONE-OK | `spec-progress` skill: live grep across non-`.context/` files returns **0 matches**. The single `.context/2026-05-12.prompt-to-skill-portability/iterate-prompt-to-skill-portability.md:20` mention describes the **problem** ("The skills table only lists `spec-progress`, missing all new skills…") — not a live reference. The skill itself was never in `skills/` (verified by `ls skills/` — no `spec-progress/` directory). | KEEP — `spec-progress` referenced only in a 2026-05 critique of the old README, which itself was subsequently updated. No current documentation lists it. |
| C15 | TOMBSTONE-OK | `b-mode` extension command: removed per `docs/buck-workflow.md:511` ("`/b-mode` and plan-mode write guards | Removed from the wired extension"). No live reference in `extensions/index.ts:6-13` wires (verified). `README.md:350` documents the removal. `scripts/install.mjs` does not list it. | KEEP — tombstone is correctly maintained. |
| C16 | KEEP | `README.md:75-79` Project Structure lists skills using example names only: `b-brainstorm, b-research, b-plan, b-build, b-iterate, b-review, b-docs, b-howto, b-save, b-memory-import, b-present, b-phase, fix-pr, git-clean-orphans, git-commit, b-grill*, run-in-idle-pane, …`. The `b-grill*` glob omits `b-grill-auto` is implicit (extension dir, not skill-only). All other listed skills exist under `skills/`. The trailing `…` makes it non-canonical. `AGENTS.md:75` has the same line — exact same text. The two are duplicates, not contradictory. | KEEP — both are example lists, neither claims completeness. |
| C17 | KEEP | `docs/extension-loading.md:1-296` is the canonical extension-loading truth table. Line 49-100 describes the `commands/` vs `prompts/` discrepancy and the 2026-09-18 mirror contract; line 102-129 lists every wired extension with `(wired)` / `(unwired)` tags matching `extensions/index.ts:6-13`. Line 159-177 confirms OMP sub-directory auto-discovery list. All consistent with `package.json`, `extensions/index.ts`, and `scripts/install.mjs`. | KEEP — the most precise catalog; cite as ground truth for any catalog conflict. |
| C18 | CATALOG-GAP | The Quick Reference Table at `docs/buck-workflow.md:301-460` partially duplicates the Runtime package mapping at `docs/buck-workflow.md:38-75`. Net overlap: ~30 components appear in both. The Quick Reference Table adds slash-entrypoint + one-line purpose; the Runtime package mapping adds Pi/OMP primitives + backing files. | Optional MERGE — collapse by adding columns. Not blocking; both are accurate. |
| C19 | KEEP | The "Extension (Runtime Hooks)" subsection at `README.md:342-350` lists 7 runtime items: model auto-switch, TPS tracking, runtime commands (`/buck-loop`, `/code-review`, `/b-pr-improved`, `/b-commit-improved`, `/b-save-improved`, `/b-kamal-release`), plan-artifact bridge. Compared to `extensions/index.ts:6-13` 8 wires + inlined model auto-switch: README is missing **no wires**. | KEEP — accurate. |
| C20 | CATALOG-GAP | `README.md:107` example workflow pipeline shows `/b-build → /b-review → /b-iterate (if in-plan issues) → /b-docs (if doc impact) → /b-save → /b-commit`. This is the 6-step completion sequence. Compare to `GLOBAL_OR_PROJECT-AGENTS.md` 11-step pipeline: `b-brainstorm → b-research → b-plan → b-present → b-build → b-build-hard → b-review → b-iterate → b-docs → b-save → b-commit`. The 11-step version includes `b-brainstorm`, `b-research`, `b-plan`, `b-present`, `b-build-hard`; the 6-step completion sequence in README focuses on the post-build completion tail. | KEEP — different scopes (one is the full pipeline, one is the completion tail). Document the relationship or both will confuse new users. |
| C21 | CATALOG-GAP | `GLOBAL_OR_PROJECT-AGENTS.md` does NOT mention `/buck-loop` (the wired runner), `/b-pr-improved`, `/b-commit-improved`, `/b-save-improved`, `/b-kamal-release` (the 4 improved variants), `/code-review` (the local iteration variant), `git-clean-orphans`, `product-tour`, or any of the `omp-*` stubs. This is consistent with C6 (bootstrap is generic) but means new users following the global bootstrap will not learn about these extensions unless they read the README. | KEEP — by design (C6). The bootstrap covers workflow conventions; surface discovery lives in the README. |
| C22 | KEEP | `README.md:155-177` Per-harness behavior table lists 8 harnesses with explicit Bootstrap / Commands / Skills columns. `docs/buck-workflow.md:163-176` is identical content. `docs/extension-loading.md:228-254` has the same 8 harnesses with different per-harness framing. All three agree with `scripts/install.mjs:42-93`. | KEEP — three catalogs, one truth (the installer). |

## Keep (brief)

- README.md Skill and Prompt Template tables — primary canonical catalog of the 43 prompt-backed commands and ~53 of 64 skills.
- docs/buck-workflow.md Runtime package mapping + Quick Reference Table — per-prompt surface contract.
- docs/extension-loading.md — extension truth table, mirror contract, harness behavior.
- docs/b-flow.md — tombstone for the deleted XState `/b-flow` extension; correct as-is.
- scripts/install.mjs HARNESSES — installer source of truth for 8 harnesses.
- plugins/buck-workflow/.codex-plugin/plugin.json + bundled skills/ — curated Codex subset, test-enforced parity with canonical skills/.
- Tombstone lists in README.md:350 and docs/buck-workflow.md:510-515 — consistent, complete, match the live `extensions/index.ts` wire surface.
- AGENTS.md:20-25 "Bootstrap vs Project AGENTS.md" — explicit design that GLOBAL_OR_PROJECT-AGENTS.md stays generic and avoids surface catalogs.

## Open questions

1. **C9 fix urgency**: `README.md:294` "b-diagnose (skill-only)" annotation is incorrect — `prompts/b-diagnose.md` exists and `/b-diagnose` is in the Prompt Templates table above (line 237). Either delete the duplicate Skills-table row or correct the parenthetical.
2. **C5 fix urgency**: `AGENTS.md:94-100` "Available Skills" lists 3 of 64 skills. Is this intentional curation or a stale doc fragment? If intentional, rename to "Notable Skills" or add a pointer to the full catalog.
3. **C2 fix urgency**: README prompt-templates table omits 5 prompt entries (`b-build-hard`, `b-docs`, `omp-goal`, `omp-workflow`, `omp-orchestrate`) that exist in `prompts/` and `commands/`. After the Extension-Backed + Pure Prompt sub-tables cover 7 of 13, these 5 are in neither. Decide whether to consolidate sub-tables or add explicit rows.
4. **C18 fix urgency**: `docs/buck-workflow.md` Quick Reference Table partially duplicates the Runtime package mapping. Pure duplication, not contradiction. Decide whether to consolidate.
5. **C20 fix urgency**: README's 6-step completion pipeline (line 107) and GLOBAL_OR_PROJECT-AGENTS.md's 11-step pipeline (Buck Workflow Steps) describe different scopes (completion tail vs full pipeline). Add a one-line note clarifying the relationship, or users may confuse them.
