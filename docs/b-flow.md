# b-flow (removed)

XState `/b-flow` was unwired on 2026-06-01 and deleted on 2026-09-20.

Use `/buck-loop` to run an existing Buck plan. `b-plan` recommends `omp_execution`; `b-phase` writes it on new phase files. Do not restore `extensions/b-flow/` or add `xstate`.

Deprecation record: `.context/2026-06-01.deprecate-b-flow/`. Source lived in git history under `extensions/b-flow/`.
